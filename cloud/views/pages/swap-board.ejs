<!DOCTYPE html>
<html lang="en">

<head>
    <link href='http://fonts.googleapis.com/css?family=Nunito' rel='stylesheet' type='text/css'>
    <% include ../partials/head %>

</head>

<body>
    <div class="banner">
        <h4 class="brand">PARCOOL</h4>
    </div>
    <div class="container-fluid main-container">
        <div class="title-header row">
          <span>Swap Board</span>
        </div>
        <div class="row">
          <div class="list-group">
            <% for(var i=0; i<requests.length; i++) { %>
              <a href="/swap/<%=requests[i].swap_id%>" class="list-group-item">
                <div class="row">
                  <span class="request-group-name-label col-xs-7"><%=requests[i].group%></span>
                  <span class="request-date-label col-xs-5 text-right"><%=requests[i].date%></span>
                </div>
                <div class="row">
                  <span class="request-message-label col-xs-7"><strong><%=requests[i].curr_driver%> - </strong><%=requests[i].note%></span>
                  <% if(requests[i].is_active) { %>
                  <span class="col-xs-5 text-right"><span class="label label-pending">Pending</span></span>
                  <% } else { %>
                  <span class="col-xs-5 text-right"><span class="label label-complete">Complete</span></span>
                  <% } %>
                </div>
              </a>
            <% } %>
          </div>
        </div>
    </div>
    <!-- /.container-fluid -->

<div class = "navbar navbar-default navbar-fixed-bottom">
	<div class  ="container">
		<% include ../partials/footer %>
	</div>
</div>

</body>

</html>
